Install a http server to run any webapp

npm install http-server -g
or
npm install ws -g

goto APM > index.html

Run a http server
------------------
open cmd > type 'ws' or 'http-server' (preinstalled node packages)